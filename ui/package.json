{
  "name": "secd-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./index.html --outdir dist --target browser --production --sourcemap=none --entry-naming [name].[ext] --chunk-naming [name].[ext] --asset-naming [name].[ext]",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@noble/ciphers": "2.3.0",
    "@noble/curves": "2.3.0",
    "@noble/hashes": "2.3.0"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
